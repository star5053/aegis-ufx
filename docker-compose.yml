services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: aegis_ufx
    volumes:
      - aegis_pg:/var/lib/postgresql/data

volumes:
  aegis_pg:
