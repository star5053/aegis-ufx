import { redirect } from "next/navigation";

/** Reels experience is the Home For You surface — keep route for Create sheet links. */
export default function ReelsRedirectPage() {
  redirect("/ufx");
}
